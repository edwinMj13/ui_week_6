import 'package:flutter/material.dart';

class Orders_Two extends StatelessWidget {
  const Orders_Two({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Orders")),

    );
  }
}
