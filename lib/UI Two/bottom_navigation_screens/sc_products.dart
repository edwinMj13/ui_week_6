import 'package:flutter/material.dart';

class Products_Two extends StatelessWidget {
  const Products_Two({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Products")),

    );
  }
}
