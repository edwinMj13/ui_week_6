import 'package:flutter/material.dart';

class Accounts_Two extends StatelessWidget {
  const Accounts_Two({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text("Accounts")),

    );
  }
}
